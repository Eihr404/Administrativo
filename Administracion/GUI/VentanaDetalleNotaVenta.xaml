<UserControl x:Class="Administracion.GUI.VentanaDetalleNotaVenta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid x:Name="dnvGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="dnvTxtblBuscarCodigo" Width="250" VerticalContentAlignment="Center" ToolTip="Buscar por Número de Nota o Producto"/>
            <Button x:Name="dnvBtnConsultar" Content="Consultar" Width="90" Margin="5,0,0,0" Click="dnvBtnConsultar_Click"/>
            <Button x:Name="dnvBtnIngresar" Content="Ingresar" Width="90" Margin="5,0,0,0" Click="dnvBtnIngresar_Click"/>
            <Button x:Name="dnvBtnModificar" Content="Modificar" Width="90" Margin="5,0,0,0" Click="dnvBtnModificar_Click"/>
            <Button x:Name="dnvBtnEliminar" Content="Eliminar" Width="90" Margin="5,0,0,0" Click="dnvBtnEliminar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dnvDatGri" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  SelectionMode="Single" 
                  IsReadOnly="True" 
                  MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número Nota" Binding="{Binding NdvNumero}" Width="Auto"/>
                <DataGridTextColumn Header="Cód. Producto" Binding="{Binding ProCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding DnvCantidad}" Width="Auto"/>
                <DataGridTextColumn Header="Monto Detalle" Binding="{Binding DnvMonto}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border x:Name="PanelFormularioDnv"
                Grid.Row="2"
                Margin="0,10,0,0"
                Padding="15"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="6"
                Background="#FAFAFA"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="GESTIÓN DETALLE NOTA DE VENTA" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Margin="5">
                        <Label Content="Nota de Venta:"/>
                        <ComboBox x:Name="cmbNotaVenta" DisplayMemberPath="NdvNumero" SelectedValuePath="NdvNumero"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5">
                        <Label Content="Producto:"/>
                        <ComboBox x:Name="cmbProductoDnv" DisplayMemberPath="ProNombre" SelectedValuePath="ProCodigo"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                        <Label Content="Cantidad:"/>
                        <TextBox x:Name="txtDnvCantidad"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                        <Label Content="Monto:"/>
                        <TextBox x:Name="txtDnvMonto"/>
                    </StackPanel>
                </Grid>

                <UniformGrid Columns="2" Margin="0,15,0,0">
                    <Button Content="Guardar" Margin="5" Click="BtnGuardar_Click" Background="#DFF0D8"/>
                    <Button Content="Cancelar" Margin="5" Click="BtnCancelar_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>