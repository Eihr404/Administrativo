<UserControl x:Class="Administracion.GUI.VentanaNotaVenta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="850">
    <Grid x:Name="ndvGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="ndvTxtblBuscarNumero" Width="250" VerticalContentAlignment="Center" ToolTip="Buscar por número de nota"/>
            <Button x:Name="ndvBtnConsultar" Content="Consultar" Width="90" Margin="5,0,0,0" Click="ndvBtnConsultar_Click"/>
            <Button x:Name="ndvBtnIngresar" Content="Ingresar" Width="90" Margin="5,0,0,0" Click="ndvBtnIngresar_Click"/>
            <Button x:Name="ndvBtnModificar" Content="Modificar" Width="90" Margin="5,0,0,0" Click="ndvBtnModificar_Click"/>
            <Button x:Name="ndvBtnEliminar" Content="Eliminar" Width="90" Margin="5,0,0,0" Click="ndvBtnEliminar_Click"/>
        </StackPanel>

        <DataGrid x:Name="ndvDatGri" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  SelectionMode="Single" 
                  IsReadOnly="True" 
                  MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número NV" Binding="{Binding NdvNumero}" Width="Auto"/>
                <DataGridTextColumn Header="Sucursal" Binding="{Binding SucCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Cod. Pedido" Binding="{Binding PddCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Fecha Emisión" Binding="{Binding NdvFechaEmision, StringFormat=\{0:dd/MM/yyyy\}}" Width="Auto"/>
                <DataGridTextColumn Header="Monto Total" Binding="{Binding NdvMontoTotal}" Width="Auto"/>
                <DataGridTextColumn Header="Responsable" Binding="{Binding NdvResponsable}" Width="Auto"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding NdvDescripcion}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border x:Name="PanelFormularioNdv"
                Grid.Row="2"
                Margin="0,10,0,0"
                Padding="15"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="6"
                Background="#FAFAFA"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="GESTIÓN DE NOTA DE VENTA" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Margin="5">
                        <Label Content="Número Nota:"/>
                        <TextBox x:Name="TxtNdvNumero"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5">
                        <Label Content="Sucursal:"/>
                        <ComboBox x:Name="CboSucursal" 
                          DisplayMemberPath="Nombre" 
                          SelectedValuePath="SucCodigo"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="5">
                        <Label Content="Pedido:"/>
                        <ComboBox x:Name="CboPedido" 
                          DisplayMemberPath="PddCodigo" 
                          SelectedValuePath="PddCodigo"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                        <Label Content="Fecha Emisión:"/>
                        <DatePicker x:Name="DtNdvFecha"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                        <Label Content="Monto Total:"/>
                        <TextBox x:Name="TxtNdvMontoTotal"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="5">
                        <Label Content="Responsable (CI):"/>
                        <TextBox x:Name="TxtNdvResponsable"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="5">
                        <Label Content="Descripción:"/>
                        <TextBox x:Name="TxtNdvDescripcion" Height="40" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>
                </Grid>

                <UniformGrid Columns="2" Margin="0,15,0,0">
                    <Button Content="Guardar" Margin="5" Click="BtnGuardar_Click" Background="#DFF0D8"/>
                    <Button Content="Cancelar" Margin="5" Click="BtnCancelar_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>