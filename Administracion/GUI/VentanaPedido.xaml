<UserControl x:Class="Administracion.GUI.VentanaPedido"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="800">
    <Grid x:Name="pddGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Ingrese el código del registro a buscar:" 
                Margin="0,0,0,5" 
                FontWeight="SemiBold" 
                Foreground="#555"/>
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="pddTxtblBuscarCodigo" 
                      Width="250" 
                      VerticalContentAlignment="Center" 
                      ToolTip="Buscar por codigo de Pedido"/>
                <Button x:Name="pddBtnConsultar" Content="Consultar" Width="90" Margin="5,0,0,0" Click="pddBtnConsultar_Click"/>
                <Button x:Name="pddBtnIngresar" Content="Ingresar" Width="90" Margin="5,0,0,0" Click="pddBtnIngresar_Click"/>
                <Button x:Name="pddBtnModificar" Content="Modificar" Width="90" Margin="5,0,0,0" Click="pddBtnModificar_Click"/>
                <Button x:Name="pddBtnEliminar" Content="Eliminar" Width="90" Margin="5,0,0,0" Click="pddBtnEliminar_Click"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="pddDatGri" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  SelectionMode="Single" 
                  IsReadOnly="True" 
                  MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding PddCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Cédula Cliente" Binding="{Binding CliCedula}" Width="Auto"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding PddEstado}" Width="60"/>
                <DataGridTextColumn Header="Ubicación" Binding="{Binding PddUbicacion}" Width="150"/>
                <DataGridTextColumn Header="Monto Total" Binding="{Binding PddMontoTotal}" Width="Auto"/>
                <DataGridTextColumn Header="Abono" Binding="{Binding PddAbono}" Width="Auto"/>
                <DataGridTextColumn Header="Comentario" Binding="{Binding PddComentario}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border x:Name="PanelFormularioPdd"
                Grid.Row="2"
                Margin="0,10,0,0"
                Padding="15"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="6"
                Background="#FAFAFA"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="DETALLE DE PEDIDO" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Margin="5">
                        <Label Content="Código Pedido:"/>
                        <TextBox x:Name="TxtPddCodigo"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5">
                        <Label Content="Cédula Cliente:"/>
                        <TextBox x:Name="TxtCliCedula"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                        <Label Content="Estado (A/I):"/>
                        <TextBox x:Name="TxtPddEstado" MaxLength="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                        <Label Content="Ubicación:"/>
                        <TextBox x:Name="TxtPddUbicacion"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="5">
                        <Label Content="Comentario:"/>
                        <TextBox x:Name="TxtPddComentario" Height="40" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0" Margin="5">
                        <Label Content="Monto Total:"/>
                        <TextBox x:Name="TxtPddMontoTotal"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="1" Margin="5">
                        <Label Content="Abono:"/>
                        <TextBox x:Name="TxtPddAbono"/>
                    </StackPanel>
                </Grid>

                <UniformGrid Columns="2" Margin="0,15,0,0">
                    <Button Content="Guardar" Margin="5" Click="BtnGuardar_Click" Background="#DFF0D8"/>
                    <Button Content="Cancelar" Margin="5" Click="BtnCancelar_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>